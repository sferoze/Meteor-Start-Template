# To use npm packages on the front end, cd into the '.npm' hidden folder. 
# Then run command 'npm install package-name', and update package.json file
# After the package has sucessfully been installed open entrypoint.js
# Add a require statement requiring that package you installed
# Then run command 'grunt build' in the '.npm' folder 
# This will compile file into Meteors lib directory
# The package will be available as a global variable as defined in entrypoint.js

